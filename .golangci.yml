# CI / 本地 golangci-lint 共用配置（lint 在各自 module 目录执行：根仓库跑 ./common/... 会 typecheck 失败）
# go-zero 使用 json:",optional" 等标签，会误报 staticcheck SA5008
run:
  timeout: 5m

linters-settings:
  staticcheck:
    checks:
      - all
      - -SA5008
