# 设备服务（默认 -f etc/device.yaml）；监听与 @host 注释一致时可改为 8002。
Name: device-api
Host: 0.0.0.0
Port: 8002

# 浏览器跨域（对应 common/cors.Config）。
CORS:
  AllowCredentials: false
  AllowOrigins:
    - "*"
  AllowMethods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
  AllowHeaders:
    - "*"

Auth:
  AccessSecret: audio-ai-platform-user-secret
  AccessExpire: 3600

Postgres:
  DataSource: "postgres://admin:admin123@14.103.202.69:5432/audio_platform?sslmode=disable&TimeZone=Asia/Shanghai"

Redis:
  Addr: "14.103.202.69:6379"
  Password: "cjs110017"
  DB: 0
