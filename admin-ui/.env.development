# just a flag
ENV = 'development'

# base api：开发环境建议留空（走 9527 同源 + 代理到 8000）。
# 切勿填 /api 或 /api/v1：请求 URL 已含 /api/v1，否则会拼成 /api/api/v1/... 或 /api/v1/api/v1/... 导致 404。
# 直连后端填 http://127.0.0.1:8000（不要带 /api 或 /api/v1 后缀）。
VUE_APP_BASE_API =

